#include <unistd.h>

/**
 *
 */

int main (void)
{
	char endOf_file = 1;

	while (endOf_file == 1)
	{
		if (endOf_file)
		{
			//
			//features into facts.
			//lev. (CHRONOS AND SYNCROS).
			endOf_file = 0;
		}
		else
		{
			return (1);
		}
	}
	return (0);
}
